@model IEnumerable<Proyecto_21351029.Request>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.account_number)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.request_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_requested)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.class_code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.account_number)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.request_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_requested)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.class_code)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.request_code }) |
            @Html.ActionLink("Details", "Details", new { id=item.request_code }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.request_code })
        </td>
    </tr>
}

</table>
